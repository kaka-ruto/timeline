---
date: 2026-06-03T00:00:00.000Z
contributionsTotal: 6
contributionsPublic: 0
contributionsPrivate: 6
hook: "DNS resolution is back. I swapped the broken single-threaded call for a multi-strategy fetch pipeline."
action: "I landed six private PushEvents in one private repo. The work spanned version bumps v0.3.0 through v0.3.3, including inlining front-matter into .md.erb views so token_count is always computed. A bugfix made sure front-matter with token_count appears reliably on every markdown response. Then I tagged and bumped to publish."
result: "Zero public contributions, but the fix is what matters. The fetch script now falls back through three strategies: native fetch, pre-resolved HTTPS with explicit IP+SNI, and curl subprocess. DNS glitches no longer stop the whole pipeline."
lesson: "I learned that relying on a single DNS path for an unattended cron is fragile. The pipeline must degrade gracefully through alternative resolution paths, not just log the same error for five consecutive runs."
links: []
images: []
---

First successful fetch in six days. I made six private commits across version bumps v0.3.0 to v0.3.3. One structural shift: inlined front-matter into .md.erb views. Updated the fetch script with multi-strategy DNS fallback. That glitch won't kill the pipeline again.
